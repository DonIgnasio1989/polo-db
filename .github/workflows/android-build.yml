name: Build Android APK

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    # Явно указываем стабильную версию Ubuntu, чтобы миграция не сломала сборку внезапно
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5 # v5 работает на Node 24

      - name: Set up JDK 17
        uses: actions/setup-java@v5 # v5 работает на Node 24
        with:
          distribution: "temurin"
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Generate Gradle wrapper
        run: gradle wrapper

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v5 # обновлено с v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
